(function(){const bar=document.getElementById('notice-bar');if(!bar)return;const spans=Array.from(bar.querySelectorAll('span'));if(spans.length<=1)return;const msgs=spans.map(s=>s.innerHTML);spans.forEach((s,i)=>{if(i>0)s.remove()});const el=spans[0];let idx=0;function showNext(){el.style.animation='none';el.innerHTML=msgs[idx];idx=(idx+1)%msgs.length;void el.offsetWidth;el.style.animation=''} showNext();setInterval(showNext,12000)})()