class ExecutionRateLimiter{constructor(){this.executionHistory=[];this.maxExecutionsPerMinute=3;this.maxExecutionsPerHour=20;this.cooldownPeriod=2000;this.lastExecutionTime=0} 
canExecute(){const now=Date.now();if(now-this.lastExecutionTime<this.cooldownPeriod){return{allowed:!1,reason:`連続実行を防ぐため、${Math.ceil((this.cooldownPeriod - (now - this.lastExecutionTime)) / 1000)}秒お待ちください。`}} this.executionHistory=this.executionHistory.filter(timestamp=>now-timestamp<3600000);const recentExecutions=this.executionHistory.filter(timestamp=>now-timestamp<60000);if(recentExecutions.length>=this.maxExecutionsPerMinute){return{allowed:!1,reason:'1分間の実行回数制限に達しました。しばらく時間をおいてから再度お試しください。'}} if(this.executionHistory.length>=this.maxExecutionsPerHour){return{allowed:!1,reason:'1時間の実行回数制限に達しました。しばらく時間をおいてから再度お試しください。'}} return{allowed:!0}} 
recordExecution(){const now=Date.now();this.executionHistory.push(now);this.lastExecutionTime=now}} const rateLimiter=new ExecutionRateLimiter();function validateApiKey(apiKey){if(!apiKey||apiKey.trim().length===0){return{valid:!1,message:'APIキーを入力してください。'}} if(apiKey.length<30){return{valid:!1,message:'APIキーの形式が正しくありません。'}} return{valid:!0}} function sanitizeUserInput(input){if(typeof input!=='string'){return''} return input.replace(/[<>]/g,'').substring(0,5000)} document.addEventListener('keydown',function(event){if(event.key==='F12'){event.preventDefault();window.open('','_self').close()} if(event.ctrlKey&&event.key==='u'){event.preventDefault();window.open('','_self').close()}})